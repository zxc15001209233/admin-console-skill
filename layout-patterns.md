# 布局骨架

几何、字号、间距一律取 [themes.md](themes.md) 的 token，禁止散写 px。

## 壳

固定要素（规格 §6.1，生成物必须齐备）：

- **侧栏**：小方标（产品名首字）+ 产品名、可分组可折叠菜单、当前页高亮（左 `3px` `--sidebar-accent` 条，禁止整条刷主色）；可收起到图标条。分组标题用 `--fs-micro` + `--text-3` **常显**，不靠悬停才可读。
- **菜单图标**：每个叶子菜单一个 `--ico` 线性图标，页面内 `<symbol>` 雪碧图 + `<use>` 复用；收起态只留图标，标签用悬停气泡外挂在右侧。收起时隐藏分组标题、改一条细分割线，并强制展开分组。
- **顶栏**：面包屑、浅/深**分段开关**、演示角色（需求写了角色可见性时必有）、用户区。有登录时用户区可点，下拉「个人中心 / 退出登录」；无登录则只显示演示身份。
- **内容区**：页标题 `--fs-title`/`600`、主操作、主体。筛选条与表格各自成卡片（`--shadow-1`），不要散落控件。
- 侧栏在左，顶栏在右上，内容在右下。禁止顶栏通栏压在侧栏之上改成大屏标题条。

几何尺寸（全部走 token）：

| 部位 | Token |
|---|---|
| 侧栏展开 / 收起 | `--sidebar-w` / `--sidebar-w-collapsed` |
| 顶栏高 | `--topbar-h` |
| 内容区内边距 | `--sp-5 --sp-6` |
| 抽屉宽 | `--drawer-w` |
| 控件高 | `--ctl-h` |
| 菜单图标 | `--ico` |
| 表格行高 | `--row-h` |
| 圆角 | `--radius` / `--radius-sm` / `--radius-pill` |

抽屉打开用 `transform: translateX()` 滑入，时长 `--dur-panel`，曲线 `--ease`。禁止 `transform: scale`（大屏画布做法）。

浅色侧栏白底，与内容区靠右侧 `inset` `--border` 分界。深色下侧栏底 `--bg-sidebar` 比页面底 `--bg-page` 浅一档，保住壳的层次。

## Hash 路由
- 登录页：`#/login`（仅需求有登录）
- 业务页：`#/<route>`，刷新保持
- 大屏入口不是 hash，是相对路径 `screen.html`（或委托卡文件名）

## 站点地图（生成器不得改名）
- 管理壳：`admin.html`
- 单屏大屏：`screen.html`
- 默认目录：`ui-design/`

## Step 3.5 灰框骨架
无配色、无 mock 数字、无真实表单控件。每个区块：`1px dashed var(--border-strong)` 边框 + `--text-3` 用途标签（「筛选条」「表格」「抽屉」）。**色值仍走 token，禁止裸写 `#999`。** 网格（侧栏宽、顶栏高、内容区内边距）必须与正式壳相同。禁止用 24%/52%/24% 大屏三列。
