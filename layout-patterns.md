# 布局骨架

几何、字号、间距一律取 [themes.md](themes.md) 的 token。壳尺寸禁止散写 `28px` / `3px`（用 `--ctl-sm` / `--accent-bar` / `--ring-w`）。列宽可按语义定宽。

## 壳

固定要素（规格 §6.1，生成物必须齐备）：

- **侧栏**：小方标（产品名首字）+ 产品名、可分组可折叠菜单、当前页高亮（左 `var(--accent-bar)` `--sidebar-accent` 条，禁止整条刷主色）；可收起到图标条。分组标题用 `--fs-micro` + `--text-3` **常显**，不靠悬停才可读。
- **菜单图标**：每个叶子菜单一个 `--ico` 线性图标，页面内 `<symbol>` 雪碧图 + `<use>` 复用；收起态只留图标，标签用悬停气泡外挂在右侧。收起时隐藏分组标题、改一条细分割线，并强制展开分组。
- **顶栏**：左侧面包屑（产品名 / **当前页**），右侧太阳/月亮**分段开关**、演示角色（需求写了角色可见性时必有，视觉用 `.demo-role` 降级）、用户区。有登录时用户区可点，下拉「个人中心 / 退出登录」；无登录则只显示演示身份。不要去掉面包屑只留右侧。
- **多页签**：紧挨顶栏下方。点侧栏叶子菜单则打开或切到该页签。首页（工作台；没有则站点第一个业务页）不可关；其余可 × 关掉，关掉当前页则切到相邻页签。登录 / 忘记密码无页签；退出登录清空页签。
- **页签样式**：与顶栏同 `--bg-topbar`。**顶栏与页签之间、页签与内容之间各一条** `inset 0 -1px 0 var(--border)`，禁止删掉中间那条。文字 `--fs-body`，左右 padding 与顶栏同 `--sp-6`。选中不铺 `--bg-page`、不做圆角卡片，只用底部 `--accent-bar` 主色细条。悬停只改字色。
- **缓存**：叶子菜单配 `cache` / 路由 `meta.keepAlive`。默认列表 `true`（切走再回来保留未点查询的输入），表单/向导 `false`。关页签销毁该页 DOM。
- **内容区**：当前页名由页签承担，**不要在页签下再放 `.page-head h1`**。主操作（新建）放筛选条内。筛选条与表格各自成卡片（`--shadow-1`）。筛选按钮紧跟筛选项。
- 侧栏在左，顶栏在右上，页签在顶栏下，内容在右下。禁止顶栏通栏压在侧栏之上改成大屏标题条。

几何尺寸（全部走 token）：

| 部位 | Token |
|---|---|
| 侧栏展开 / 收起 | `--sidebar-w` / `--sidebar-w-collapsed` |
| 顶栏高 | `--topbar-h` |
| 内容区内边距 | `--sp-5 --sp-6` |
| 抽屉宽 | `--drawer-w` |
| 控件高 | `--ctl-h` |
| 菜单图标 | `--ico` |
| 表格行高 | `--row-h` |
| 小控件 / 头像 / 分页钮 | `--ctl-sm` |
| 当前菜单左条 | `--accent-bar` |
| 焦点环宽 | `--ring-w` |
| 确认弹窗宽 | `--modal-w` |
| 圆角 | `--radius` / `--radius-sm` / `--radius-pill` |

抽屉打开用 `transform: translateX()` 滑入，时长 `--dur-panel`，曲线 `--ease`。禁止 `transform: scale`（大屏画布做法）。

浅色侧栏白底，与内容区靠右侧 `inset` `--border` 分界。深色下侧栏底 `--bg-sidebar` 比页面底 `--bg-page` 浅一档，保住壳的层次。

## Hash 路由
- 登录页：`#/login`（仅需求有登录）；右上角与顶栏同一套太阳/月亮开关；右栏默认 `.login-scene` 淡圆，有图才 `--login-photo`
- 未知 hash：404 空态，不改写成默认业务页
- 业务页：`#/<route>`，刷新保持
- **列表筛选 / 分页 / 树选中写入 query**：`#/rooms?q=一号&from=2026-09-01&page=2`。刷新、深链登录回跳后筛选项仍在。用 `URLSearchParams` 读写，禁止只放内存
- 详情深链仍用 path：`#/students/12`。关抽屉收回列表时**带上**离开前的 query
- **未登录深链**：记下完整 hash（含 query）→ `#/login` → 登录成功回跳该 hash
- **退出登录**：清登录态，`pendingHash = ''`，`resetTabs()`，去 `#/login`。**禁止**回跳退出前的页
- 登录 / 忘记密码：`resetTabs()`，不画页签
- 大屏入口不是 hash，是相对路径 `screen.html`（或委托卡文件名）

## 站点地图（生成器不得改名）
- 管理壳：`admin.html`
- 单屏大屏：`screen.html`
- 默认目录：`ui-design/`

## Step 3.5 灰框骨架
无配色、无 mock 数字、无真实表单控件。每个区块：`1px dashed var(--border-strong)` 边框 + `--text-3` 用途标签（「页签条」「筛选条」「表格」「抽屉」）。**色值仍走 token，禁止裸写 `#999`。** 网格（侧栏宽、顶栏高、页签条、内容区内边距）必须与正式壳相同。禁止用 24%/52%/24% 大屏三列。
