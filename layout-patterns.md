# 布局骨架

## 壳

固定要素（规格 §6.1，生成物必须齐备）：

- **侧栏**：产品名、可分组可折叠菜单、当前页高亮；可收起到图标（展开 `232px`，收起 `64px`）。
- **顶栏**：面包屑、浅/深切换、演示角色（需求写了角色可见性时必有）、用户区（无登录则演示身份，如「演示用户」）。
- **内容区**：页标题、主操作、主体。
- 侧栏在左，顶栏在右上，内容在右下。禁止顶栏通栏压在侧栏之上改成大屏标题条。

几何尺寸：

- 左侧栏宽度展开 `232px`，收起 `64px`
- 顶栏高度 `56px`
- 内容区内边距 `20px 24px`

## Hash 路由
- 登录页：`#/login`（仅需求有登录）
- 业务页：`#/<route>`，刷新保持
- 大屏入口不是 hash，是相对路径 `screen.html`（或委托卡文件名）

## 站点地图（生成器不得改名）
- 管理壳：`admin.html`
- 单屏大屏：`screen.html`
- 默认目录：`ui-design/`

## Step 3.5 灰框骨架
无配色、无 mock 数字、无真实表单控件。每个区块：`1px dashed #999` 边框 + 用途标签（「筛选条」「表格」「抽屉」）。网格（侧栏宽、顶栏高、内容区）必须与正式壳相同。禁止用 24%/52%/24% 大屏三列。
