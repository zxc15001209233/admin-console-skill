# Mock 数据规则

管理壳内所有页的假数据与假交互遵守本文件。与 [page-patterns.md](page-patterns.md) 积木配合使用。

## 硬性规则

所有管理页共用一个内联 `MOCK` 对象。禁止「用户1」「测试数据」。

`const USE_MOCK = true;` 与字段名一起出现。禁止写真实 API 路径、HTTP 错误码、DDL。

混合项目：与大屏确认单/委托卡上的共享实体同名同数。不抽公共 mock-data.js。

人名用张伟、王芳、李娜；时间相对今天。文档上的专名与锚点数字整句照搬。

`currentUser` 必须含 `name`、`username`、`role`、`roleLabel`。个人中心只读这四个字段，不读演示角色下拉。

## 代码组织

- 在 `admin.html` 内联一份 `MOCK` 对象，保证双击可开、零构建
- 列表、详情、表单、删除后的列表必须自洽（同 id 同字段）
- 字段名与确认单字段表一致，便于后续对接

```js
const USE_MOCK = true;

const MOCK = {
  rooms: [
    { id: 'r1', name: '三楼会议室 A', capacity: 12, status: '可用', updatedAt: '今天 09:12' },
    // ...
  ],
  currentUser: { name: '张伟', username: 'zhangwei', role: 'admin', roleLabel: '管理员' },
};
```

- 业务逻辑通过 `USE_MOCK` 分支读 `MOCK`；不写 `fetch('/api/...')`
- 注释标明口径（如「会议室列表，与详情共用 id」）

## 命名与量级

| 类型 | 要求 | 反例 |
|---|---|---|
| 人名 | 张伟、王芳、李娜（可轮换刘强、陈静） | 用户1、测试用户 |
| 组织 / 产品名 | 自然中文专名；混合项目与委托卡一致 | 产品A、组织1 |
| 数量 | 与业务常识匹配；尾数自然 | 12345、99999 扎堆 |
| 状态文案 | 用需求文档原文 | 随意自造同义词 |
| 时间 | 相对今天（今天、昨天、近 7 天） | 硬编码 2019 年 |

文档或图上的专名、状态文案、锚点数字整句照搬；禁止改成「差不多」的另一套。

## 混合项目共享实体

委托卡 / 联合确认单写明的共享实体（组织名、产品名、锚点数字）：

- 管理 HTML 的 `MOCK` 与大屏 HTML 的 mock **同名同数**
- 各文件可各有一份内联 `MOCK`，禁止抽 `mock-data.js`
- 联合验收时核对：名称字符串一致、锚点数值一致

## 交互与数据联动

| 操作 | mock 要求 |
|---|---|
| 筛选 / 分页 | 从 `MOCK` 过滤；总条数与分页一致 |
| 新建 / 编辑 | 写入 `MOCK` 数组；列表立即可见 |
| 删除 | `#modal` 确认后从 `MOCK` 移除 |
| 批量删除 | `#modal` 确认后批量移除；勾选状态清空 |
| 详情 | 按 id 查同一 `MOCK` 源 |
| 导入 | 可追加行或 toast；条数变化合理 |

点了只 toast、列表 / 详情不变 = P0。

## 禁止项

- 真实 API 路径（`/api/v1/users` 等）
- HTTP 状态码全文、后端错误码枚举
- DDL、`CREATE TABLE`
- 「用户1」「测试数据」「placeholder」类占位
- 与大屏各编一套互不一致的共享实体名或锚点数字
