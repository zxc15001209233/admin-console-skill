# 页面积木

生成时按确认单选用的积木名匹配本表。需求有才生成；未列积木不得静默发明页面。

## 硬性规则

表单默认抽屉。文档明确要整页或弹窗时，确认单写明，禁止静默改。

**查看与编辑必须同形态。** 既然编辑走抽屉，查看也走抽屉；要改成弹窗就两个一起改。禁止「查看跳整页详情、编辑弹抽屉」这种一半一半——同一行的两个操作把用户甩到两种容器里，是最常见的廉价感来源。

审批流页、权限矩阵页：第一版不提供模板。需求出现则进确认单缺失项，禁止发明页面。

演示角色只切换 `data-role` 与按钮/菜单 `hidden`，不是权限配置页。**不改** `MOCK.currentUser`。

工作台数字卡放在内容区顶部，禁止做成大屏 KPI 玻璃卡或 ECharts 仪表。样式口径只有一处：[themes.md](themes.md) 的「脸」一节。

深链打开演示角色看不到的页：渲染 `.empty`「没有权限」，不要把内容画出来再藏按钮。这不是权限矩阵页。

## 积木表

| 积木 | 何时用 | 形态 |
|---|---|---|
| 登录壳 | 需求有登录 | 左品牌带（`--fs-brand`，垂直居中 + `.login-watermark`）+ 右卡片叠在 `--bg-page` 上（有图才铺 `--login-photo`）；`#/login`；**右上角 `.theme-seg`** 太阳/月亮。用户图写入 `--login-photo` 并加强 `--login-scrim`。卡片标题「欢迎回来」。忘记密码页同样要有主题开关 |
| 404 | 未知 hash | `.empty`「页面不存在」+ 返回首页。禁止静默跳到默认页 |
| 表头排序 | 需求有排序 | `th.sortable` + `data-sort`，写入 hash `sort`/`dir` |
| 下拉多选筛 | 标签/楼层等多值 | `.multi-select`，选中值写入 hash |
| 假上传 | 表单有附件 | `input type="file"`，只记文件名 + toast，不写真实 URL |
| 只读列表 | 日志 / 审计 | 有筛选分页，无新建/编辑；行操作最多「查看」 |
| 主从两表 | 单据头+明细 | 上表选中行，下表刷明细；两表同一 MOCK |
| 整页设置 | 系统参数 | `#/settings` 整页表单，不是抽屉 |
| 树搜索 | 树节点很多 | 树顶关键字，过滤节点标签 |
| 列表 | 查询 / CRUD | 见下方「列表」 |
| 表单 | 新建 / 编辑 | 默认右侧 `#drawer`；字段很多或分步则整页（确认单写明） |
| 详情 | 只读查看 | 与编辑同形态（默认同一 `#drawer`）；描述列表；主操作「编辑」「关闭」 |
| 确认弹窗 | 删除 / 解锁 / 批量删除 | `#modal`，禁止 `window.confirm` |
| 空态/错误 | 任何列表或提交 | 三段式空态；校验失败保留已填；无权限用同一空态 |
| 工作台数字卡 | 需求有工作台 | 内容区顶部一行数字卡（标题 + 数值 + 可选环比） |
| 树+表 | 组织 / 分类 | 左树（`--tree-w`）右表；点树节点筛表；范围条按需；树选中写入 hash |
| 导入导出 | 需求写了 | 列表工具栏按钮 + 假进度条 / 结果 toast |
| 演示角色 | 需求写了角色可见性 | 顶栏 `.demo-role`（标「演示」）；只改 `data-role` 显隐，不改登录身份 |
| 批量操作条 | 需求写了批量删/改 | 勾选列 + `.batch-bar` |
| 日期范围筛 | 需求有起止日期 | `.date-range` 两个 `type="date"` |

## 列表

必备结构（自上而下）：

1. **页标题行**：标题 + 主按钮「新建」
2. **筛选条**：关键字、状态等；「查询」「重置」紧跟在筛选项后面（左对齐）。禁止用 flex spacer 把按钮甩到最右
3. **表格**：表头（无底色 + `--border-strong` 下边线）+ 行操作（查看 / 编辑 / 删除）
4. **分页**：`共 N 条` + 页码；当前页、总条数与表格数据一致；上一页/下一页视觉重量不得压过内容
5. **行操作**：查看 / 编辑为文字链，且**打开同一种容器**（默认都是抽屉）；删除为危险色文字链（不要描边红按钮）
6. **列宽按语义分级**：`table-layout: fixed`，主体列（名称）留 `width:auto` 吃掉剩余宽度，短码/性别/状态/操作定宽；编号与数值列 `tabular-nums`。禁止所有列一律同一个 `max-width`，也禁止把富余宽度平均摊给每一列

交互：

- 筛选 / 搜索 / 分页：表格立即变，条数与分页一致；**写入 hash query**（见 [layout-patterns.md](layout-patterns.md)），刷新可复现
- 新建：打开表单积木（默认 `#drawer`）
- 删除：打开 `#modal` 确认，确认后从列表消失并 toast。禁止 `window.confirm`
- 批量删除（需求有才做）：勾选列 + `.batch-bar`（已选 N 项 + 操作）；确认走 `#modal`
- 日期范围（需求有才做）：筛选条里 `.date-range`
- 提交按钮可加 `.is-loading` 防连点；mock 用短延时去掉
- 无数据或筛空：显示空态积木，禁止空白表

## 表单

- 默认：右侧抽屉，宽 `--drawer-w`，`translateX` 滑入；遮罩点击不关闭（防误触，因此**不要**挂无用的 `stopPropagation`）
- 标题区与正文之间一条 `--border` 分割；底部操作条吸底
- 字段：按确认单字段表；必填标 `*`；校验失败留在表单
- 提交成功：关闭抽屉，列表出现 / 更新该行，toast
- 整页表单：确认单写明原因（字段多、分步）；路由如 `#/rooms/new`
- 弹窗表单：确认单写明；禁止静默把抽屉改成弹窗。形态用 `#modal`（宽 `--modal-w`），不要另起一套

## 确认弹窗

删除、解锁、批量删除、覆盖导入：必须用 `#modal`。

- 居中卡片，宽 `--modal-w`，遮罩 `--bg-overlay`，`z-index` 高于抽屉
- 标题 + 一句说明 + 取消 / 确定。确定若为破坏性操作用危险色实心按钮
- Esc、点取消关闭；**点遮罩不关闭**（与抽屉一致，防误触）
- 打开时焦点进确定或取消；Tab 循环留在弹窗内
- 禁止 `alert` / `confirm` / `prompt`

## 批量操作条

需求写了批量才生成。结构：

1. 表头勾选列（`.c-check`）+ 行勾选
2. 有勾选时在表上方显示 `.batch-bar`：`已选 N 项` + 操作按钮（批量删除等）
3. 点操作仍走 `#modal`，确认后改 `MOCK` 并清勾选

禁止只在工具栏放「批量删除」却没有勾选列。

## 日期范围筛

需求有起止日期才生成。`.date-range`：两个 `input type="date"`，中间「至」。写入 hash 的 `from` / `to`。样式走 `.field`，不要浏览器原生刺眼日历皮肤以外的自造色。

## 详情

- **与编辑同形态**：编辑是抽屉，详情就是同一个抽屉的只读态，共用标题栏、宽度、进出动效
- 描述列表展示只读字段；长文本可换行
- 底部操作：「关闭」+「编辑」（原地切到表单态，不关抽屉不跳页）；删除放左侧危险色文字链
- 深链 `#/<route>/:id` 仍要能用：渲染列表后自动把抽屉打开在只读态；关闭时把 hash 收回列表
- 详情字段与列表 / mock 同一实体，禁止对不上

## 空态/错误

- 列表无数据：居中三段式 —— 线性空态图标 + 小标题（`--fs-title`）+ 说明（`--fs-body` / `--text-3`）+ 主操作按钮
- 筛空：小标题写「没有匹配结果」，说明提示调整筛选条件，操作给「重置筛选」
- 提交校验失败：字段下错误提示，保留已填内容
- 无权限（演示角色看不到的路由）：小标题「没有权限」，说明「当前演示角色看不到此页」，不要渲染表格再藏按钮
- 禁止整页白屏、禁止只有一行灰字、禁止只有表头无行且无提示

## 工作台数字卡

- 放在内容区顶部（页标题下或同行），横向排列 2–4 张；行宽 `--stat-max`，不要在超宽屏拉满
- 每张：指标名（`--fs-label` / `--text-2`）、数值（`--fs-num` / `600` / `tabular-nums`）、可选环比文案（`--fs-label` / `--text-3`）
- 样式用 `--bg-card` + `--shadow-1` + `--radius`，**不加边框、不加主色竖条**；悬停可微抬升
- 禁止玻璃拟态、发光描边、ECharts 仪表
- 下方可接列表或其他积木

## 树+表

- 左侧树：宽 `--tree-w`；组织 / 分类层级；选中项用 `--color-primary-soft` + 左 `var(--accent-bar)` 主色条
- 右侧表：与列表相同结构（筛选条 + 表头 + 分页）
- 点树节点：表格按节点过滤；根节点显示全部；选中 key 写入 hash（如 `tree=class:c1`）
- **范围条按需**，不是每棵树+表都带。仅当当前节点有要展示的只读摘要字段时，才放在右表上方、筛选条之前。没有摘要就不要空条。
- 形态：`.scope-bar` 靠左排布、`flex-wrap: wrap`、项与项用 `--sp-6` 间隙；每项 `.scope-item` 内 `.k` 与 `.v` **同一行**（标签在左、值在右）。禁止均分整行，禁止标签在上、值在下。例：`年级 初三　　班级 初三（1）班　　班主任 李娜　　人数 4`

## 导入导出

- 入口在列表工具栏（「导入」「导出」）
- 导入：选文件 → 假进度 → 成功 / 失败 toast（可更新列表行数）
- 导出：点击 → 假下载提示 toast
- 禁止写真实上传 URL 或后端错误码

## 演示角色

- 顶栏下拉，选项来自确认单角色表
- 切换后：`document.body` 或壳根节点设 `data-role`；按角色隐藏按钮 / 菜单项（`hidden` 或等效 class）
- **不是登录身份**：不改 `MOCK.currentUser`，个人中心不读这颗下拉
- 不是权限配置页；不生成角色编辑、权限矩阵、审批流页
- 手改 hash 进入隐藏页：走无权限空态

## 身份模型

`MOCK.currentUser` 是顶栏姓名、个人中心、登录后身份的**唯一展示源**：

```js
currentUser: { name: '张伟', username: 'zhangwei', role: 'staff', roleLabel: '员工' }
```

| 字段 | 用途 |
|---|---|
| `name` | 顶栏与个人中心「姓名」 |
| `username` | 个人中心「账号」；登录成功可按输入更新 |
| `role` | 登录角色 key（与业务账号一致） |
| `roleLabel` | 个人中心「角色」文案 |

演示角色下拉只覆盖预览显隐，**允许**和 `currentUser.role` 不一致（这是开关，不是切换账号）。禁止把下拉当前项写进个人中心。

## 登录用户区

需求有登录时必做（无登录则顶栏只显示演示身份，不出现退出）：

- 点击头像（或姓名）弹出菜单，两项：**个人中心**、**退出登录**。资料不堆在菜单里
- 再点一次、点菜单外、Esc 关闭
- **个人中心**：独立 `#profileDrawer`，只读姓名 / 账号 / 角色（读 `currentUser`），底栏只有「关闭」。不进侧栏，不发明改密、换头像
- 业务 `#drawer` 已打开时点个人中心：**不要**覆盖业务抽屉的标题/正文/编辑态；个人中心叠在上面（更高 z-index）。关掉个人中心后，业务抽屉与 hash（如 `#/students/:id`）保持原样
- **退出登录**：关菜单与浮层，清 `sessionStorage` 登录态，`pendingHash = ''`，回 `#/login`。与「未登录深链要回跳」相反，禁止复用深链回跳逻辑
- Esc 关闭顺序：用户菜单 → `#modal` → `#profileDrawer` → 业务 `#drawer`
- 浮层 `--bg-card` + `--shadow-2`，随浅/深变；退出用危险色文字链

## 登录增强（仅确认单写了才做）

验证码、失败锁定、忘记密码不是默认登录积木。PRD 写了再按确认单做，字段与文案照搬；没写就不要发明。禁止每份原型各造一套验证码皮肤。

默认登录：账号 + 密码 + 提交。任意账号可进的演示环境，在确认单写明。

登录页和忘记密码页必须在 **`.login-wrap` 右上角**放 `.theme-seg`（太阳 / 月亮，`aria-label` 浅色 / 深色），与顶栏共用 `applyTheme` + `admin-theme:<产品名>`。壳顶栏的开关在登录时被藏掉，禁止只靠那一颗。用 `themeSegHtml()` 拼进登录/忘记密码 markup，点 `[data-theme-set]` 走同一套 `applyTheme`（同步所有开关的 `.active`）。

右栏 `.login-main` 默认 `--bg-page`（`--login-photo: none`）。有图时铺 `--login-photo`，上面叠 `--login-scrim`，卡片 `z-index` 压住遮罩。禁止默认光晕、点阵、折角、假建筑剪影、营销大图。确认单或入口③给了图，把 `--login-photo` 改成 `url("相对路径")` 或 `url("https://...")`，并把 `--login-scrim` 加深（浅 `.48` / 深 `.55`）。左栏不放照片，**垂直居中**，方标字做 `.login-watermark`。登录卡片标题用「欢迎回来」，按钮仍写「登录」。

## 404

未知 hash（已登录）：渲染三段式空态，小标题「页面不存在」，主操作「返回首页」（产品默认页）。**禁止** `navigate` 到工作台/列表假装该页存在。未登录仍先回 `#/login`，登录后再落到 404。

## 表头排序

需求写了排序才生成。`th.sortable` 可点，箭头用 `--text-3`。当前列写入 hash：`sort=name&dir=asc`。排序在筛选之后做。禁止每列都排序；编号、操作列不要排序。

## 下拉多选筛

单选不够时用 `.multi-select`：按钮长得像 `.field`，点开多选项（checkbox），选中后按钮文案为「已选 N」或已选标签。值写入 hash（逗号分隔）。不要用原生 `select multiple`。

## 假上传

表单有附件时：`input type="file"`。选择后 toast 文件名，把名字记在 MOCK 字段上。禁止 `fetch` 上传、禁止对象存储 URL。

## 只读列表

日志、流水、审计：筛选 + 表 + 分页，没有新建。行操作不要「编辑」「删除」；需要时只留「查看」。确认单积木名写「只读列表」，不要套成 CRUD。

## 主从两表

上表单据头，点行后下表显示该行明细。两个表都走同一套表头/行高 token。下表空态：「先选择一条单据」。选中 id 写入 hash。

## 整页设置

系统参数、偏好：`#/settings` 整页表单（确认单写明），底栏保存。不要塞进抽屉。保存改 MOCK + toast。

## 表头吸顶

`.table-wrap` 设 `max-height` 并 `overflow:auto`，`th` `position:sticky; top:0; background: var(--bg-card)`。底用卡片色，**禁止** `--bg-muted` 灰表头。

## 树搜索

树节点超过一屏时，树顶加关键字框，过滤节点文案。无匹配时树区域用短空态，不要空白。
