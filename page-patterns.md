# 页面积木

生成时按确认单选用的积木名匹配本表。需求有才生成；未列积木不得静默发明页面。

## 硬性规则

表单默认抽屉。文档明确要整页或弹窗时，确认单写明，禁止静默改。

审批流页、权限矩阵页：第一版不提供模板。需求出现则进确认单缺失项，禁止发明页面。

演示角色只切换 `data-role` 与按钮/菜单 `hidden`，不是权限配置页。

工作台数字卡放在内容区顶部，禁止做成大屏 KPI 玻璃卡或 ECharts 仪表。

## 积木表

| 积木 | 何时用 | 形态 |
|---|---|---|
| 登录壳 | 需求有登录 | 居中卡片，无侧栏；`#/login`；登录后回跳原 hash |
| 列表 | 查询 / CRUD | 见下方「列表」 |
| 表单 | 新建 / 编辑 | 默认右侧抽屉；字段很多或分步则整页（确认单写明） |
| 详情 | 只读查看 | 描述列表；主操作「编辑」「返回」 |
| 空态/错误 | 任何列表或提交 | 空态文案 + 插图位；校验失败保留已填 |
| 工作台数字卡 | 需求有工作台 | 内容区顶部一行数字卡（标题 + 数值 + 可选环比） |
| 树+表 | 组织 / 分类 | 左树（约 `240px`）右表；点树节点筛表 |
| 导入导出 | 需求写了 | 列表工具栏按钮 + 假进度条 / 结果 toast |
| 演示角色 | 需求写了角色可见性 | 顶栏下拉；切换后按钮 / 菜单显隐立刻变 |

## 列表

必备结构（自上而下）：

1. **页标题行**：标题 + 主按钮「新建」
2. **筛选条**：关键字、状态等；「查询」「重置」
3. **表格**：表头 + 行操作（查看 / 编辑 / 删除）
4. **分页**：当前页、每页条数、总条数与表格数据一致

交互：

- 筛选 / 搜索 / 分页：表格立即变，条数与分页一致
- 新建：打开表单积木（默认抽屉）
- 删除：必须先 `confirm`，确认后从列表消失并 toast
- 批量删除（若有）：先 `confirm`，再批量移除
- 无数据或筛空：显示空态积木，禁止空白表

## 表单

- 默认：右侧抽屉，宽度约 `480px`；遮罩点击不关闭（防误触）
- 字段：按确认单字段表；必填标 `*`；校验失败留在表单
- 提交成功：关闭抽屉，列表出现 / 更新该行，toast
- 整页表单：确认单写明原因（字段多、分步）；路由如 `#/rooms/new`
- 弹窗表单：确认单写明；禁止静默把抽屉改成弹窗

## 详情

- 描述列表展示只读字段；长文本可换行
- 主操作：「编辑」（进表单）、「返回」（回列表 hash）
- 详情字段与列表 / mock 同一实体，禁止对不上

## 空态/错误

- 列表无数据：居中空态文案 + 可选「新建」引导
- 筛空：提示调整筛选条件
- 提交校验失败：字段下错误提示，保留已填内容
- 禁止整页白屏或只有表头无行且无提示

## 工作台数字卡

- 放在内容区顶部（页标题下或同行），横向排列 2–4 张
- 每张：指标名、数值、可选环比 / 同比文案
- 样式用 `--bg-card` + `--border`，禁止玻璃拟态、发光描边、ECharts 仪表
- 下方可接列表或其他积木

## 树+表

- 左侧树：组织 / 分类层级；选中高亮
- 右侧表：与列表相同结构（筛选条 + 表头 + 分页）
- 点树节点：表格按节点过滤；根节点显示全部

## 导入导出

- 入口在列表工具栏（「导入」「导出」）
- 导入：选文件 → 假进度 → 成功 / 失败 toast（可更新列表行数）
- 导出：点击 → 假下载提示 toast
- 禁止写真实上传 URL 或后端错误码

## 演示角色

- 顶栏下拉，选项来自确认单角色表
- 切换后：`document.body` 或壳根节点设 `data-role`；按角色隐藏按钮 / 菜单项（`hidden` 或等效 class）
- 不是权限配置页；不生成角色编辑、权限矩阵、审批流页
