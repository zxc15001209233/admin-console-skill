# 独立审核清单

生成后的自检（Step 5）不能代替本清单。审核目标是**找不一致**，禁止把自检勾选复制成结论。

审核结论只写生成物同级 `review/{主文件名}-审核-YYYYMMDD.md`，**禁止**写入设计说明正文。

有任意 P0 未清（修不掉）：不准声称本侧 HTML 可交付。混合项目未做联合验收：不准声称「混合项目已完成」。

**对照范围**：确认单、站点地图、硬性规则、本清单、[mock-data-rules.md](mock-data-rules.md)、[themes.md](themes.md)。

---

## 单边 P0

出现任一条即为 P0（未清不得声称本侧 HTML 可交付）：

- 确认单中的页面/字段缺失
- JS 报错
- 深色浮层看不清
- 列表与详情数据对不上
- href 与站点地图字符串不一致
- 直连/委托门禁用错
- 确认单要求数据变化但只 toast

**禁止**：因对端 HTML 尚未存在判 404。写「对端文件待生成」。

---

## 联合验收 P0（仅混合收口）

两边 HTML 都齐，或用户宣布混合原型收口时，才执行本节。出现任一条即为 P0：

- 对端 404
- 缺站点地图
- 共享实体被改写

---

## 单边审核补充（混合项目先落地一边时）

- 只核 href / 「返回管理」与站点地图**字符串一致**
- 对端文件尚未存在：**不是**本侧 P0；审核正文写「对端文件待生成」
- 共享实体、实际跳转是否 404：**留到联合验收**，不在单边判 P0

---

## P1（应修；修不完须在审核里写明并复审）

- 筛选/搜索/分页后条数与表格行数不一致
- 删除/批量删除后列表仍显示已删行
- 校验失败时表单被清空
- hash 刷新丢页；列表筛选/分页未写入 hash query；有登录时深链未回登录或未回跳原 hash
- 退出登录后又回跳到退出前的页
- 个人中心复用业务 `#drawer`，或角色文案读演示下拉而不是 `currentUser.roleLabel`
- 删除/解锁仍用 `window.confirm`
- 演示角色（若有）切换后按钮/菜单显隐未立刻变
- 空态缺失（筛空或列表无数据时空白表）
- 主题切换后表头/输入/抽屉/弹窗/下拉未随主题变（未达 P0「看不清字」但仍不一致）
- 长文本溢出、异常横向滚动
- 色值/圆角/阴影/字号/间距未走 [themes.md](themes.md) token（裸写 `#fff`、`red`、`#2f6fed`，或壳几何散写 `28px` / `3px`）
- 精工脸退化：卡片又套边框、表头刷回灰底、状态只改字色、每张数字卡挂主色竖条、当前菜单整条实心主色、所有列一律同一个 `max-width`
- 侧栏：菜单缺图标或只有一半有图标、收起态没有悬停气泡（只剩看不懂的图标）、当前项没用 `--sidebar-accent` 左条（浅色可与主色同值，深色禁止把 `--color-primary` 铺到深底）
- 查看与编辑不同形态（查看跳整页 / 编辑弹抽屉）
- 交互态缺失：无 hover、无 `:focus-visible` 焦点环、无过渡（键盘 Tab 看不出焦点）
- `node scripts/check-template.js <生成物路径>` 未跑或未通过
- 套用了营销落地页、大屏玻璃卡、`design-taste-frontend` 默认脸
- 混合项目：大屏被 iframe 进管理内容区，或侧栏大屏项未用相对链接整页打开

---

## P2（能力边界；标明「未覆盖」即可，不必循环改）

- 移动端适配
- 审批流页、权限矩阵页（应已在确认单缺失项，不得静默发明）
- 与参考图在色值/圆角/间距上的细微差异（信息架构对齐即可，不承诺像素复刻）
- 控件皮肤与将来 Element Plus 实现的 1:1 一致（本 Skill 只出 HTML 原型）

用户说「不要再打磨 / 停」：停止改观感，进入 Step 6。

---

## Step 5 与 Step 5.5 对照

| Step 5 自检项 | Step 5.5 额外核什么 |
|---|---|
| 壳完整 | 对照确认单：每页积木是否齐全 |
| token / 浅深各过一遍 | 深色浮层是否 P0；是否裸写色值或散写 px；精工脸是否退化 |
| 交互态 | hover / focus-visible / active 是否齐，键盘 Tab 焦点是否可见 |
| checker | 是否对**生成物路径**跑过 `check-template.js` 并为 `OK` |
| 无横滚、长文本省略 | 是否影响可读或布局破功 |
| 空态与校验 | 是否只 toast 不变数据（P0） |
| hash / 登录深链 | 与确认单交互清单一致；筛选 query；退出不回跳 |
| 登录用户区 | `#profileDrawer` 独立；个人中心读 `currentUser` |
| 演示角色 | 与确认单角色显隐表一致；不改 `currentUser` |
| mock 自洽 | 列表 ↔ 详情 ↔ 表单提交后列表 |
| 站点地图 href | 字符串与确认单一致；混合单边不要求对端文件存在 |
| 无 JS 报错 | 控制台未捕获错误 |

Step 5 **不能代替** Step 5.5。审核报告禁止复述 Step 5 勾选而无具体差异。

---

## 建议取证

- 浏览器控制台：打开管理页、切换 hash、筛选/新建/删除/主题切换，确认无未捕获报错
- 浅色 + 深色各截图一页含浮层（抽屉/弹窗/下拉）
- 对照确认单：页面清单、字段表、交互清单逐项打勾或列差
- 混合单边：贴站点地图片段 + 侧栏 href 字符串；对端未生成则写「对端文件待生成」
- 联合验收：实际点击相对链接、核对共享实体名称与数量

---

## 审核文件骨架（照抄后填）

```markdown
# {产品名} 管理壳审核

- 日期：YYYY-MM-DD
- 对象：{如 ui-design/admin.html}
- 启动路径：直连 | 委托
- 对照：确认单 / 站点地图 / 硬性规则 / 本清单

## 结论

不通过（修不掉：…） | 本侧通过（P0/P1 已修完复审） | 混合联合验收通过 | 混合联合验收不通过

## 单边 P0

- …

## 联合验收 P0（仅混合收口时填）

- …

## P1 / P2

- …

## 混合备注

- 对端文件：已存在 | 待生成（写文件名）
- href 与站点地图：一致 | 不一致（…）

## 证据

- 控制台：…
- 截图：…
- 对照项：…

## 复审（改完后追加）

- 日期：
- 仍未清：
```
