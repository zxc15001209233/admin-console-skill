# 后台管理生成器 · 产品使用指南

> 面向产品、业务同学。不需要懂代码，会用 Cursor 对话即可。
>
> 这个工具能帮你们**快速生成中后台管理页的 HTML 原型**：统一侧栏/顶栏壳、可点假交互、浅/深主题可切换，适合评审、演示、对齐 CRUD 需求。

---

## 一、你能得到什么

| 产出 | 说明 | 适合干什么 |
|------|------|------------|
| **HTML 单文件**（默认 `admin.html`） | 双击用浏览器打开，不用装软件 | 给业务看列表/表单/详情流程，对齐菜单与字段 |
| **【后台方案确认单】** | 生成前 AI 先发你确认 | 核对菜单、页面、缺失项、疑点，避免理解偏了白做 |
| **设计说明** | 菜单 ↔ 页面 ↔ 字段对照 | 和开发对接时当说明附件 |
| **审核报告** | 生成物旁边的 `review/` 文件夹 | 了解还有哪些没做到、是否可交付 |

**暂时做不到的（提前知道，避免误会）：**

- 不会自动连**真实接口**（只有 mock 假数据 + 字段名占位）
- 不会生成**审批流页**、完整**权限矩阵页**（文档写了会进确认单「缺失项」，不静默发明）
- 有参考图时：只辅助**信息架构**（菜单、字段、分区），**不承诺像素复刻**现有后台皮肤
- **本工具不转 Vue**；你说「开始开发 / 转 Vue」会交回 `requirements-to-dev`，由开发计划阶段建工程

---

## 二、使用前准备

### 1. 确认 Skill 已安装

开发同学需要先在 Cursor 里装好 `admin-console-generator`。装好后，你在对话里提到「后台管理」「中后台」「列表页」「CRUD」等，AI 会按规范走确认单 → 生成流程。

若 AI 没出确认单就直接写文件，让开发检查 Skill 是否安装成功。

### 2. 想清楚三件事（不用很专业）

1. **管什么业务**：会议室、客户、订单、设备……能说出主体就行
2. **有哪些页面**：列表、新建/编辑、详情、工作台……列几个关键词即可
3. **有没有参考**：旧系统截图、PRD、Word 都可以

---

## 三、四种开始方式（选一种最顺手的）

### 方式 ① 一句话描述（最适合小白）

直接说业务场景，**不用自己设计菜单框架**。

**示例：**

```
帮我做一个会议室预约后台原型。
```

```
做一个客户管理中后台，要能列表查询、新建客户、看详情。
```

AI 会先给出**【后台方案确认单】**（业务识别 + 默认菜单/页面包），你确认后再生成。此方式**不要求**仓库里已有需求规格。

---

### 方式 ② 自己描述布局/菜单

你已经想好侧栏菜单、每页放什么时用。

**示例：**

```
中后台菜单：
- 工作台（数字卡）
- 会议室：列表 + 抽屉表单新建/编辑 + 详情
- 预约记录：列表 + 筛选
顶栏要演示角色切换（管理员 / 普通员工）
```

只在缺信息时 AI 才会追问。

---

### 方式 ③ 发参考图

有旧系统截图、原型图时，**把图片发给 AI**。

**示例：**

```
按这张截图做后台 HTML 原型，菜单和字段尽量对齐，脸用统一壳，数据用假数据。
```

**注意：**

- 图用于确认**菜单结构、字段、操作**，不是抠像素复刻配色
- 图糊或字段读不清时，AI 会列入确认单「缺失项」，不会假装量准

---

### 方式 ④ 发需求文档（产品经理常用）

把 PRD、Word 发给 AI，或说明文档路径。若规格已人审并走委托流程，AI 会引用委托卡上的站点地图。

**示例：**

```
按这份需求文档里的管理模块生成 HTML 原型。
文档没写清的操作列到确认单缺失项，不要自己猜。
```

**重要规则（对你有利）：**

- 文档里**嵌的图片**比纯文字更准——布局以图为准，文字管字段与交互
- 文档前后矛盾，AI 会列**疑点清单**问你，不会默默选一个
- 文档里**既有大屏又有管理页**：管理页走本工具，大屏走 `bi-dashboard-generator`（可同目录互跳，见下文）

---

## 四、确认单：生成前必看

即使用户先说「可以 / go」，**确认单未发出也不得生成**。确认单至少包括：

| 字段 | 你要看什么 |
|------|------------|
| 启动路径 | 直连（一句话也能做）还是委托（须已审规格） |
| 业务识别 / 产品名 | AI 理解对不对 |
| 生成目录 | 默认 `ui-design/` |
| 站点地图 | 有哪些 HTML；混合项目含大屏文件名 |
| 积木 / 关键字段 / 交互 | 每页做什么 |
| 主题 | 默认浅色，顶栏可切换 |
| 演示角色 | 有角色可见性时会写 |
| 缺失项 / 疑点清单 | 文档没写的、有矛盾的 |

**你怎么回复：**

- 没问题：`可以，按确认单生成`
- 要改：`菜单加一项「审批记录」先列缺失项，不要发明页面`
- 混合项目：确认 `admin.html` 与 `screen.html`（或文档里写死的名字）在同目录

---

## 五、标准流程（你只需要参与 2～3 步）

```
你提需求
   ↓
AI 出【后台方案确认单】  ← 你要看、要改、要说「可以」
   ↓
（复杂项目）灰框骨架  ← 可选，先看区域划分
   ↓
AI 生成 admin.html + 设计说明
   ↓
AI 自检 + 独立审核（review/）
   ↓
交付：双击打开 HTML
```

| 步骤 | 你要做的 | 怎么说 |
|------|----------|--------|
| 看确认单 | 查菜单、字段、缺失项、疑点 | 「可以，按确认单生成」 |
| 看成品 | 浏览器打开 HTML | 双击 `ui-design/admin.html` |
| 要修改 | 说清楚改哪页哪字段 | 「列表加一列状态，编辑用抽屉」 |
| 满意了 | 告诉开发可对接 | 「原型 OK，可以走开发计划」 |

---

## 六、生成后怎么查看与操作

### 打开方式

1. 在 Cursor 对话里，AI 会告诉你路径（一般在 **`ui-design/admin.html`**）
2. **双击**该文件，用 Chrome / Edge 打开
3. 无需启动本地服务、无需安装依赖

### 浅 / 深主题切换

- 顶栏有主题切换按钮
- 切换后当前页、抽屉、弹窗、下拉应一起变；刷新后仍保持
- 若深色下弹出层白底或字看不清，属于生成质量问题，应让 AI 修后再交付

### 页面怎么点

- 侧栏菜单切换管理页（地址栏 hash 会变，如 `#/rooms`）
- 列表可筛选、分页、新建/编辑（默认抽屉）、删除（有确认）
- 数据是 mock，但交互路径应对齐确认单

### 混合项目：整页跳大屏

若确认单里写了大屏（如 `screen.html`）：

- 侧栏里「数据大屏」类菜单会**整页打开**大屏 HTML（不是嵌在管理内容区里）
- 大屏顶栏有「返回管理」回到 `admin.html`
- 两边可以**分先后生成**；都齐后再做联合验收（链接不 404、共享名称数字一致）

同目录通常还有：

- `review/admin-审核-日期.md` — 审核结论
- `设计说明-xxx.md` — 菜单与字段对照，可转给开发

---

## 七、推荐话术模板（复制即用）

### 从零开始

```
用 admin-console-generator 帮我做一个【业务名称】后台 HTML 原型。
核心页面：【列表 / 表单 / 详情 …】
先出方案确认单，我确认后再生成。
```

### 有参考图

```
按附件截图做中后台 HTML 原型。
提取菜单和字段，壳用统一风格，先出确认单。
```

### 有需求文档

```
按附件文档中的管理模块生成 HTML 原型。
缺失项和疑点清单列在确认单里问我。
```

### 混合（管理 + 大屏）

```
这份文档既有 CRUD 又有经营分析大屏。
管理页用 admin-console-generator，大屏用 bi-dashboard-generator。
同目录 ui-design/，站点地图写进一份确认单。
```

### 迭代修改

```
在上一版 admin.html 基础上：
1. 【具体改动1】
2. 【具体改动2】
改完重新自检和 review。
```

### 暂停打磨

```
样式可以先这样，不要再抠细节了。
```

---

## 八、常见问题

**Q：为什么 AI 先给确认单，不直接生成？**  
A：避免菜单/字段理解偏了白做。你点头后再写 `admin.html`。

**Q：和参考图长得不完全一样？**  
A：第一版对齐**信息架构**，脸用统一壳和 token，不承诺像素复刻。

**Q：数据是真的吗？**  
A：不是。全是 mock，用于演示。真实接口要开发后续对接。

**Q：能做审批流、权限配置页吗？**  
A：第一版不做完整审批流页/权限矩阵页；文档写了会进缺失项。角色按钮显隐用顶栏「演示角色」切换演示。

**Q：说「转 Vue / 开始开发」会怎样？**  
A：本工具停，交回 `requirements-to-dev` 走开发计划；**不会**在本 Skill 里建 Vite 工程。

**Q：文档里还有数据大屏，会一起做吗？**  
A：大屏走 `bi-dashboard-generator`；管理页走本工具。混合项目用同目录相对链接互跳。

**Q：只有管理页先好了，大屏还没生成，算失败吗？**  
A：不算。单边可先交付管理 HTML；大屏好了再联合验收链接与共享数据。

---

## 九、给开发同学的衔接说明（产品可转发）

产品确认 HTML 原型 OK 后，开发侧通常需要：

1. 用**设计说明**对齐菜单、字段、交互
2. 按 `requirements-to-dev` 补：数据模型、接口契约、开发计划
3. 再建 Vue 业务工程（Element Plus），**不承诺**与 HTML 控件皮肤 1:1

**产品不需要自己操作第 2、3 步**，保存好最终版 `admin.html`、确认单和设计说明即可。

---

## 十、最小上手示例（第一次建议这样试）

在 Cursor 新建对话，发送：

```
帮我做一个「会议室预约」后台 HTML 原型。
要有会议室列表、预约记录、抽屉表单。
先出方案确认单。
```

确认单没问题后回复：

```
可以，按确认单生成 HTML。
```

生成完成后双击 **`ui-design/admin.html`** 打开。顶栏试一下浅/深切换。满意就存档；不满意就用第七节话术继续改。

---

## 十一、相关文件（给想深入了解的人）

| 文件 | 内容 |
|------|------|
| `SKILL.md` | AI 执行的完整技术规范 |
| `review-checklist.md` | 独立审核清单（单边 vs 联合验收） |
| `themes.md` | 浅/深 token |
| `layout-patterns.md` | 壳、hash、站点地图 |
| `page-patterns.md` | 列表、表单、详情等积木 |

---

**文档版本**：2026-09-09  
**适用 Skill**：admin-console-generator  
**维护**：开发同学更新 Skill 后，可同步更新本指南
